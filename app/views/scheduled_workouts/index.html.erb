<% @workouts.each do |workout| %>
  <%= div_for workout do %>
    <%= link_to "Scheduled Workouts ##{workout.id}", scheduled_workout_url(workout.id) %>
    <%= link_to "delete", workout, method: :delete, remote: true %><br>
  <% end %>
<% end %>

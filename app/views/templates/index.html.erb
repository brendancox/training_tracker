<% @templates.each do |template| %>
  <%= div_for template do %>
    <%= link_to "Template ##{template.id}", template_url(template.id) %>
    <%= link_to "delete", template, method: :delete, remote: true %><br>
  <% end %>
<% end %>
